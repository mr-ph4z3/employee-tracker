from tkinter import *
from tkinter import ttk

root = Tk()
root.title("Employee Management System")
root.geometry(f"{int(root.winfo_screenwidth() / 2)}x{int(root.winfo_screenheight() / 2)}")

home_content = Frame(root)

root.mainloop()
